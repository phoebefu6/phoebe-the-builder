# Pinned to the versions this build was actually executed and tested against on 2026-09-20,
# read off the interpreter that ran evidence.py, pytest and nbconvert - not copied from a
# neighbouring build. The scaffold's inherited pins (numpy 2.1.3, scipy 1.14.1, pandas 2.2.3,
# matplotlib 3.9.2, streamlit 1.39.0, pytest 8.3.3) were all wrong here; every one named a
# release that was never installed on this machine.
numpy==1.26.4
scipy==1.17.1
pandas==2.1.4
matplotlib==3.11.0
streamlit==1.58.0
pytest==7.4.0
