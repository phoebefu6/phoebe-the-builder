# Pinned to the versions this build was actually run and tested against
# (python 3.11.11 on darwin) - checked with pip, not copied from a neighbouring build.
numpy==1.26.4
scipy==1.17.1
matplotlib==3.11.0
streamlit==1.58.0
pytest==7.4.0
